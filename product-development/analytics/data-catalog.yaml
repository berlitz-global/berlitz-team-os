# Data Catalog
# Registry of warehouse tables. For column-level detail, see schemas/ docs.
#
# Entry format:
#   table_name:
#     database: DATABASE_NAME
#     schema: SCHEMA_NAME
#     description: What this table contains
#     owner: team-name
#     refresh: daily | streaming (Snowpipe) | hourly | etc.
#     upstream: Source system → pipeline tool
#     used_by: [list, of, use, cases]
#     schema_doc: schemas/area/table_name.md   # optional, link to column docs

tables:
  # Add tables here
